import styled from "styled-components";

export const Container = styled.main`
    width: 80vw;
    margin: auto;
    
    img {
        display:block;
        width: 100%;
        margin: auto;

        cursor: pointer;
    }
`;
